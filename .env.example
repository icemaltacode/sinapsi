# === Sinapsi Backend Example Environment ===
# Copy to `.env` and adjust per environment.

# SNS alert email for quota/usage notifications
USAGE_ALERT_EMAIL=alerts@example.com

# Seed admin user (created during Serverless deploy)
ADMIN_USER_EMAIL=admin@icecampus.com
ADMIN_USER_DISPLAY_NAME="ICE Campus Admin"
ADMIN_USER_ID=admin
ADMIN_TEMP_PASSWORD=ChangeMe123!

# Default student temporary password
DEFAULT_USER_TEMP_PASSWORD=Student1234!

# Default usage/quota caps
DEFAULT_OPENAI_MONTHLY_TOKENS=200000
DEFAULT_OPENAI_MONTHLY_SPEND_GBP=50

# Dynamo single-table prefix for provider secrets
PROVIDER_SECRET_PREFIX=/sinapsi/dev/providers/

# Avatar bucket (provisioned by CloudFormation)
AVATAR_BUCKET=sinapsi-avatars-dev

# AWS CLI / Serverless profile
AWS_PROFILE=sinapsi-prod
